!!!
%html
	%head
		%title= "Locating…"
		= javascript_include_tag 'application.js', 'geo.min'
		%meta{:name => "viewport", :content => "width=device-width,minimum-scale=1.0 maximum-scale=1.0"}
		:javascript
			$(document).ready(function() {
				if (geo_position_js.init()) {
				  return geo_position_js.getCurrentPosition(function(pos) {
				    window.location = "/locate?lat=" + pos.coords.latitude + "&lng=" + pos.coords.longitude
				  });
				} else {
					window.location = "/?utm_campaign=window_sticker&utm_medium=qrcode&utm_source=qrcode"
				}
			});
	%body